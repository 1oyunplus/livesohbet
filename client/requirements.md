## Packages
firebase | Backend-as-a-Service for Auth, Database, and Realtime features
framer-motion | Complex animations for the "Viral Social App" feel
lucide-react | Beautiful icons (already in stack, but noting usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes without conflicts
date-fns | Formatting timestamps for chat and last active status

## Notes
Firebase config will need to be replaced with real credentials.
Using local storage for "mock" auth if Firebase fails (for demo purposes).
Glassmorphism implemented via backdrop-blur and semi-transparent backgrounds.
App structure: Mobile-first tab bar navigation, desktop sidebar.
